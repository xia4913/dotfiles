if [[ -n $TMPDIR ]] && [[ ! -w $TMPDIR ]]; then
	unset TMPDIR
fi
if [[ -z $TMPDIR ]]; then
	if [[ -d ~/tmp ]]; then
		TMPDIR=~/tmp/tmp
	else
		TMPDIR=/tmp/user-$UID
	fi
	export TMPDIR
	if ! [[ -d $TMPDIR ]]; then
		mkdir -p $TMPDIR
		chmod og= $TMPDIR
	fi
fi

HISTSIZE=1000
LISTMAX=0
